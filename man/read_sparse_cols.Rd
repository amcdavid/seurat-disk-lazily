% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ReadH5.R
\name{read_sparse_cols}
\alias{read_sparse_cols}
\title{Read specific columns from a CSC sparse matrix stored in an HDF5 group}
\usage{
read_sparse_cols(grp, col_idx)
}
\arguments{
\item{grp}{An \code{H5Group} with CSC datasets and a \code{dims}
(or \code{h5sparse_shape} / \code{shape}) attribute.}

\item{col_idx}{Integer vector of 1-based column indices to extract.}
}
\value{
A \code{\link[Matrix]{dgCMatrix}} with \code{length(col_idx)}
  columns.
}
\description{
Performs HDF5-level column slicing on a group containing \code{data},
\code{indices}, and \code{indptr} datasets (standard CSC layout).  Only
the entries for the requested columns are read from disk; the full value
and index arrays are never realised in memory.
}
\keyword{internal}
